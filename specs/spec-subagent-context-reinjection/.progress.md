# Spec: spec-subagent-context-reinjection

> Epic: [superpowers-uplift](../_epics/superpowers-uplift/epic.md)
> Plan: [plan.md](./plan.md)
> Branch: `epic/superpowers-uplift`

## Goal (from epic)

在 `SubagentStart` 事件接入时立即重新注入 spec context + iron-law 摘要，preempt superpowers issue #237。

## Acceptance Criteria (from epic)

- **Pre-check (Task 0)**: 验证 `SubagentStart` 事件在目标 Claude Code 版本已 GA — 若未 GA 整个 spec defer 直至上游 ship
- 新 hook：`SubagentStart` 调用 `subagent-context-injector.mjs`
- 注入 payload 总大小 ≤ 2 KB
- 与 SessionStart `load-spec-context.mjs` 共享一份 context 构建函数（DRY）
- 测试覆盖：mock subagent 验证收到 phase / spec / iron-law
- CHANGELOG entry

## Constraints

- Size S-M (≤15 tasks)
- 依赖 A 完成（用其 iron-law 摘要文本与生成函数）
- payload size ≤ 2KB（避免 subagent context 通胀）
- DRY: 不复制粘贴 context 构建逻辑

## Skill Discovery

- **curdx-flow:spec-workflow** (plugin): matched but not invoked (auto-active)
- **curdx-flow:curdx-core** (plugin): no match (无 state machine 大改)
- **curdx-flow:reality-verification** (plugin): no match
- **curdx-flow:communication-style** (plugin): matched but not invoked (artifact stage)
- **curdx-flow:interview-framework** (plugin): no match (epic triage 已定 goal/AC)

## Phase Transitions

### → research (2026-05-07)
- Spec switched from spec-two-stage-review (which is now completed)
- Dep A complete; ready to research
- Research focus: SubagentStart GA verification, payload shape, shared context-builder function

### → requirements (2026-05-07)
- Goal locked, 10 user stories, 15 FRs, 6 NFRs
- Pre-check Task 0 confirmed simplifies to runtime version warning (GA verified)
- 5 open design questions carried forward for /design phase

## Learnings
- SubagentStart is GA — no flag, no version gate; spec proceeds straight (research R1)
- superpowers issue #237 closed wontfix upstream — our spec fills the gap, not duplicate work
- Iron-law canonical 1-liner already centralized in `references/iron-law-verification.md` lines 8-18 — drift test is a single assertion
- SessionStart refactor must be SURGICAL (extract function only) — byte-equal baseline is the regression gate
- Universal injection in v1 (no agent_type filter) — revisit if perf data surfaces issue
- Fail-open is non-negotiable across 4 paths: state-absent, malformed-json, payload-over-budget, lib-throws
- `additionalContext` lands as `<system-reminder>` block (not system-prompt) — issue #23885 closed wontfix; not negotiable upstream
- D1 locked: `IRON_LAW_SUMMARY` hardcoded constant in lib + drift test asserting substring of reference doc — startup-fast wins over runtime-read since drift test already enforces correctness
- D2 locked: universal injection (no `agent_type` filter v1); filter is purely additive future change with zero breaking risk
- D3 locked: `<curdx-spec-context>` key:value text block (not nested JSON) — outer system-reminder framing already provided by Claude Code; nested JSON is redundant + harder to parse
- D4 locked: surgical refactor of `load-spec-context.ts` — only extract inline payload-build, preserve all 3 function signatures + handler control flow + stderr banner verbatim; byte-equal baseline is the regression gate
- Open question 5 (completed-spec behavior) settled: fail-open `{continue: true}` no injection; subagent dispatch under completed spec is coordinator-side bug, not this hook's concern
- Architectural insight: payload format is constrained by API surface — `additionalContext` is a single string field, so structured-vs-prose is really "how to format INSIDE that string", not a real format choice
- 9-file changeset (4 NEW + 5 EDIT) — adds CHANGELOG.md to original 8-file estimate; matches research-phase task count estimate (8-12 tasks)
- Task planning (2026-05-07): 14 tasks across 5 phases; POC milestone at 1.5 (hook fires + emits valid JSON); tasks 1.3 and 1.4 marked [P] (disjoint files: build-hooks.mjs vs hooks.json); 4 [VERIFY] checkpoints (1.5, 2.2, 3.4, V4/V5/V6); drift test in Phase 3 (3.3) is the R2 mitigation; byte-equal baseline check (task 2.2) immediately follows surgical refactor to catch R1 earliest; `npm run verify` is the canonical full-CI command
- Task 1.1 (2026-05-07): shared lib pure-function — `IRON_LAW_SUMMARY` constant + `BuildContextPayloadOpts` + `PayloadOverBudgetError` class + `buildContextPayload` returning JSON-stringified SessionStart shape (default) or `<curdx-spec-context>` block (`forSubagent:true`); subagent block ~100-150B with `Buffer.byteLength` enforcement against `opts.maxBytes ?? 2048`
- Task 1.2 (2026-05-07): SubagentStart hook handler — uses `runHook` for stdin parse + uniform exit, but wraps entire body in inner try/catch that emits `{continue:true}` (not silent) on every fail-open path (state-absent, malformed JSON, completed spec, payload-over-budget, unexpected throw); resolved active spec via existing `_shared/path-resolver.resolveCurrent` (design called it `lib/spec-resolver` but actual export lives in `_shared/path-resolver`) and inline `JSON.parse(readFileSync(...))` for state read (no shared `lib/state.ts` exists; matches quick-mode-guard.ts pattern); local `SubagentInjectionOutput` interface + `as unknown as HookOutput` cast keeps the shared types surface unchanged (would have required edit to `_shared/types.ts` outside task scope); `hook_event_name` filter rejects misconfigured non-SubagentStart wiring; passes `npm run typecheck`
- Task 1.3 (2026-05-07): registered `src/hooks/subagent-context-injector.ts` in HOOK_ENTRIES alphabetically (between stop-watcher and task-completed-verifier); `npm run build:hooks` bundled 19 entrypoints (was 18: 6 hooks + 13 lib); `npm run check:hooks-fresh` exits 0 — bundles match source. Note: task description said "18 entrypoints (was 17)" but live count is 19 (was 18); the +1 delta is what matters and matches expectation
- Task 2.1 (2026-05-06): D4 surgical refactor — load-spec-context.ts now Object.assigns `JSON.parse(buildContextPayload(state, specPath))` onto the existing `block` for both completion and active branches; stderr banners + readEnabledSetting + readGoalFromProgress + handler control flow + JSON.stringify form (still done by runHook on the returned object) all preserved verbatim. Source diff = 21 ins/21 del (well under 50 LOC). Lib's default branch was adjusted in same commit to use the same fallbacks load-spec-context historically used (phase ?? "unknown", taskIndex/totalTasks ?? 0, awaitingApproval === true) so byte-equal output holds for ANY state shape, not just well-typed fixtures. All 91 hook tests pass including 4 load-spec-context cases + 16 byte-equal regressions vs v6.0.6 baselines. Insertion order of keys on `block` is unchanged because Object.assign preserves ordering of pre-existing keys (active, specName, specPath) and only appends phase/taskIndex/totalTasks/awaitingApproval — same order the inline assignments produced.
- Task 3.1 (2026-05-06): unit cases a-e for subagent-context-injector — fixture stdin uses snake_case `hook_event_name:"SubagentStart"` (the hook reads `input?.hook_event_name`, not the older `hookEvent` key used by load-spec-context fixtures); single shared `with-spec.json` fixture works for all 5 cases because `_helpers.runHook` rewrites the `cwd` field at runtime to the per-test `os.tmpdir()` path. Case (b) state-absent uses `createFixtureSpec({noStateFile:true})` so `.current-spec` resolves but the state file is missing → existsSync false → `{continue:true}` no injection (stderr clean since no parse occurs). Case (c) malformed-state overwrites the default state file with `"{not valid json"` so JSON.parse throws → stderr trace prefixed `[subagent-context-injector]` + `{continue:true}`. Case (d) NFR-1 size budget: full output JSON ≤ 2048B (measured ~140B) and `additionalContext` text block ≤ 200B (measured ~120B) — well below ceiling. Case (e) drift-adjacent: imports `IRON_LAW_SUMMARY` directly from `src/hooks/lib/build-context-payload.js` and asserts substring presence, complementing the dedicated drift test in 3.3. Full hook suite: 96 passed (was 91; +5 new). No fixture rewrites needed for cases (f)-(g) yet — those land in 3.2.
- Task 3.3 (2026-05-06): drift gate — single-assertion vitest test imports `IRON_LAW_SUMMARY` from `src/hooks/lib/build-context-payload` and asserts `content.includes(IRON_LAW_SUMMARY)` against `plugins/curdx-flow/references/iron-law-verification.md`. Doc currently has the literal string `**No completion claim without fresh verification.**` (note: surrounded by `**` markdown bold markers but the constant is plain text — substring match still holds because the constant text itself appears verbatim inside the bold delimiters). Test passes (1/1) in 1ms; gate is structurally minimal (~32 LOC including header comments) and matches the iron-law-doc.test.ts style for the file pattern. Discovery: vitest config uses `pool: 'forks'` so test isolation is per-file. R2 mitigation now live — any future edit to `IRON_LAW_SUMMARY` constant or to the canonical 1-liner in the reference doc fails CI gate.
- Task 4.1 (2026-05-06): CHANGELOG entries appended to existing v7.1.7 section — three Added bullets (SubagentStart hook, shared `build-context-payload.ts` lib, 7 unit + drift + byte-equal tests) + one Changed bullet (SessionStart `load-spec-context.ts` surgical refactor — 21 ins/21 del under 50-LoC ceiling, byte-equal preserved) + one `> Note` blockquote on `SubagentStart` GA status vs `TaskCompleted` opt-in. Verify: `grep -A 30 "subagent-context-injector|build-context-payload" CHANGELOG.md | head -10` returned all three new Added bullets verbatim → CHANGELOG_PASS. Style matches existing entries: bold lead phrase, file paths in backticks, concrete numerics (~120 B, 2 KB ceiling, 21 ins/21 del, 91→99 tests). Note: task description in tasks.md (line 174) was the v7.1.7-create-from-scratch shape; user-supplied execution prompt overrode with append-to-existing-section semantics, which is what this commit implements.
- Task 3.2 (2026-05-06): cases (f)-(g) + SubagentStart byte-equal baseline. (f) uses `createFixtureSpec({state:{completed:true,completedAt:"..."}})` — hook short-circuits at `state.completed===true` guard before `buildContextPayload` even runs, so output is `{continue:true}` with **no** `hookSpecificOutput` field (asserted via `toBeUndefined()`). (g) uses `createFixtureSpec({state:{quickMode:true,phase:"design"}})` — D2 universal injection holds: assertion shape mirrors case (a), confirms no quickMode gate v1. SubagentStart byte-equal: NEW hook has no v6.0.6 baseline, so the test freezes the **current v7.1.7 output** as `SUBAGENT_START_BASELINE` constant inline (separate `describe` block at end of byte-equal.test.ts; outside the v6.0.6-loop). Frozen baseline = exactly stdout `{"hookSpecificOutput":{"hookEventName":"SubagentStart","additionalContext":"<curdx-spec-context>\\nphase: execution\\nspec: specs/demo-spec\\niron-law: No completion claim without fresh verification.\\n</curdx-spec-context>"},"continue":true}\n` + `EXIT_CODE=0\n` — drives off `/tmp/curdx-fixture-spec` provisioned by existing `provisionWorkspaces()`, so no fixture-setup churn. No `normalize()` needed (no mtimes, no ISO timestamps, no path-sep divergences in the SubagentStart output) — verbatim equality is the gate. Test count: 99 (was 96; +2 unit + 1 byte-equal). When future intentional output changes land, update `SUBAGENT_START_BASELINE` in same commit; otherwise this is the drift gate.

## Completed Tasks
- [x] 1.1 Create shared lib build-context-payload.ts
- [x] 1.2 Create subagent-context-injector.ts hook handler
- [x] 1.3 [P] Register subagent-context-injector in build-hooks HOOK_ENTRIES
- [x] 1.4 [P] Register SubagentStart event in hooks.json
- [x] 2.1 Surgical refactor load-spec-context.ts to import shared lib
- [x] 3.1 Create subagent-context-injector.test.ts — cases (a)-(e)
- [x] 3.2 Add cases (f)-(g) + SubagentStart byte-equal baseline
- [x] 3.3 Create drift test tests/runner/subagent-context-doc.test.ts
- [x] 4.1 Append CHANGELOG v7.1.7 entries — SubagentStart hook + shared lib + tests + GA note
