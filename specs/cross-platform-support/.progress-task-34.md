## Task 1.35 — markdown sweep batch 2
**Commit**: <pending>
**Verify**: BATCH2_OK observed (count=0 across research-analyst.md + references/)

**Replacements summary**:
- references/failure-recovery.md: 4 jq -> inline `node -e` (preserves `attempts`/`fixTaskIds`/`lastError` schema documented in spec.schema.json)
- references/coordinator-pattern.md: 3 jq -> 1 prose update (lib/merge-state.mjs reference) + 1 inline `node -e` for modificationMap mutation + 1 inline `node -e` for PROPOSED_COUNT computation via stdin
- references/quality-commands.md: 2 jq -> inline `node -e` for `package.json scripts` extraction + table cell shows curl + node-pipe alternative
- agents/research-analyst.md: 6 jq -> 5 inline `node -e` (scripts dump, dev/start/serve filter, deps filter, port extraction, scripts-keys list) + 1 lib call to `merge-state.mjs` for `awaitingApproval = true`

**Notes**:
- Did NOT use `update-fix-task-map.mjs` lib for failure-recovery.md state mutation: the lib uses `{count, depth, fixes:[{id,reason}]}` schema while the markdown docs and `spec.schema.json` use `{attempts, fixTaskIds, lastError}`. Switching to the lib would silently change on-disk schema and break the doc's own readers (which read `.attempts` and `.fixTaskIds`). Used inline `node -e` instead — preserves documented schema exactly.
- Smoke-tested all 4 inline-node snippets locally against this repo's package.json (output matches expectations) plus the failure-recovery state mutation snippet against a synthetic state file (correct output `{"attempts":1,"fixTaskIds":["1.3.1"],"lastError":"syntax err"}` and `totalTasks` increment).
- Smoke-tested the read+process-substitution pattern for the limit-check snippet (`attempts=2 max=3 history=1.3.1,1.3.2`).
- Coordinator-pattern.md prose change is faithful: "use jq merge pattern" -> "use the deep-merge pattern (via merge-state.mjs)" — same semantics, no behavior change.
- All snippets use `${CLAUDE_PLUGIN_ROOT}` for lib paths where applicable, matching the pattern used elsewhere in references/.
