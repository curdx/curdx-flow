## Task 1.31 — kill-port.ts
**Commit**: <pending>
**Verify**: KILLPORT_OK observed (port 65535 unused, exit 0 path)
**Notes**:
- Empty-result handling: `tryExec()` wraps `execFileSync` in try/catch and returns `{ stdout: "", ok: false }` on non-zero exit. `lsof -ti :65535` exits 1 with no matches; we treat that as "no PIDs" rather than propagating the failure.
- POSIX uses `lsof -ti :<port>` then `kill <pid>` (not `kill -9`, per task spec).
- Windows uses `netstat -ano` filtered by `:<port>` substring, then `taskkill /F /PID <pid>` for each unique PID (Set dedupes).
- Port validation: `Number.parseInt` + range check 1..65535; non-integer or out-of-range exits 1 to stderr.
- `stdio: ["ignore", "pipe", "pipe"]` keeps the verify command's exit code clean — no inherited stdin, captured stderr.
- Individual kill failures (already-dead PID, permission) log warning to stderr but still exit 0 if at least one kill was attempted (per task guidance).
