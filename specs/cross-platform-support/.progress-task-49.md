## Task 3.3 — hook smoke tests
**Commit**: <pending>
**Verify**: 12 tests passed; SMOKE_OK observed
**Test layout**:
- tests/hooks/_helpers.ts (spawn helper, ~95 LOC; auto-detects stdin vs invocation-spec fixtures)
- tests/hooks/load-spec-context.test.ts (3 cases: with-spec / quick-mode / malformed-stdin)
- tests/hooks/quick-mode-guard.test.ts (3 cases: quick-active / quick-inactive / malformed-stdin)
- tests/hooks/stop-watcher.test.ts (3 cases: execution-block / corrupt-state / malformed-stdin)
- tests/hooks/update-spec-index.test.ts (3 cases: with-spec / empty-quiet / bogus-args)

**Fixtures used (12 total — pre-existing 8 + 4 new error fixtures)**:
- load-spec-context: with-spec.json, quick-mode.json, **error-malformed.json (NEW)**
- quick-mode-guard: quick-active.json, quick-inactive.json, **error-malformed.json (NEW)**
- stop-watcher: execution-block.json, corrupt-state.json, **error-malformed.json (NEW)**
- update-spec-index: with-spec.json, empty-quiet.json, **error-bogus-args.json (NEW)**

**Notes**:
- v7 .mjs bundles emit JSON on stdout (load-spec-context, quick-mode-guard, stop-watcher); v6 bash baseline did not — this is a deliberate v7 enhancement, not a regression. The byte-equal regression test (3.6) compares the existing baseline format separately.
- `_helpers.ts` auto-detects fixture format: stdin-style (raw piped to stdin) vs invocation-spec `{cwd, args}` (parsed → spawn cwd + argv, stdin empty). Detection key: presence of `cwd` + `args` and absence of `hookEvent`.
- All 4 hooks honor FR-8 (never block Claude): malformed stdin / bogus argv → exit 0 with stderr diagnostic.
- update-spec-index test cleans `specs/.index/` before each run so file-side-effect assertions reflect this invocation only.
- Each test runs the actual `.mjs` bundle via spawnSync (real Node child process, not mocked), satisfying reality-verification (no mock-only tests).
