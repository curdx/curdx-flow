## Task 1.34 — markdown sweep batch 1
**Commit**: 51a272d
**Verify**: count=0 across 3 hot files (templates/tasks.md, agents/task-planner.md, commands/implement.md)
**Replacements made** (10 jq → 0):
- implement.md:89-117 → merge-state lib call (PATCH built via inline `node -e`, then `node merge-state.mjs $SPEC/.curdx-state.json $PATCH`). Chose merge-state over init-execution-state because the spec preserves earlier-phase fields (source, name, basePath); init-execution-state only writes from template and refuses to overwrite without --force.
- task-planner.md:903 → `node merge-state.mjs <basePath>/.curdx-state.json '{"awaitingApproval":true}'` (one-liner, atomic-write internal)
- templates/tasks.md:320 → `gh pr view --json statusCheckRollup | node -e "..."` (drops `--jq` flag, pipes JSON to Node stdin)
- templates/tasks.md:409 → `gh pr view --json reviews | node -e "..."` (same pattern, filter reviews by state)
- 5 prose examples (templates/tasks.md:172, :205, agents/task-planner.md:57, :572, :602) → dropped `| jq ...` suffix, kept `curl ...` intact. Educational point about API verification preserved.

**Notes**:
- `gh --jq` is technically the gh CLI's built-in gojq filter (not the system jq binary), but it still matches `\bjq\b` and would fail the verify gate. Replaced with stdin-pipe-to-Node pattern, which is cross-platform and uses no external deps.
- The merge-state lib's deep-merge handles the implement.md case correctly: existing fields like `source`, `name`, `basePath`, `commitSpec`, `relatedSpecs` are preserved while execution fields are merged in. Atomic-write internal (no tmp+mv).
- Pre-existing modifications detected outside scope: `references/failure-recovery.md` and 3 deleted `.progress-task-{29,30,31}.md` files from parallel batches; these are NOT included in this commit.
