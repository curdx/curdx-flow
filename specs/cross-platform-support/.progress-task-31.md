## Task 1.32 — state-tracking trio
**Commit**: c027758
**Verify**: TRIO_OK observed (3 bundles confirmed)
**Smoke tests**:
- init-execution-state /tmp/cflow-1.32 → wrote target, embedded template fallback used (no plugins/curdx-flow/templates/.curdx-state.template.json present in repo)
- update-modification-map /tmp/cflow-1.32 1.5 src/foo.ts src/bar.ts → `{"1.5":["src/foo.ts","src/bar.ts"]}`
- update-fix-task-map .../.curdx-state.json 1.5 1.5.fix1 "bug" → `{"1.5":{"count":1,"depth":1,"fixes":[{"id":"1.5.fix1","reason":"bug"}]}}`
- Idempotent guard: re-running init without --force exited 1 with stderr message

**Notes**:
- All three reuse `writeFileAtomic` from `_shared/atomic-write.js` — no manual tmp+rename code anywhere.
- `init-execution-state` resolution order: cwd `plugins/curdx-flow/templates/.curdx-state.template.json` → bundle-adjacent `__dirname/../../templates/...` → embedded constant. Repo currently has no template file, so the embedded constant fired and matches the spec-listed shape verbatim.
- `update-modification-map` accepts both spec-dir and explicit `.json` path arg1 (suffix sniff on `.endsWith(".json")`); writes sidecar `.file-modifications.json` when spec dir is given.
- `update-fix-task-map` depth = `dotsIn(fixId) - dotsIn(originalId)`; uses `Math.max(existing, new)` semantics via `if (newDepth > entry.depth)`.
- LOC counts (excluding header comments): update-modification-map ~40, update-fix-task-map ~55, init-execution-state ~60. Within the 30-50 envelope (init slightly over due to template fallback chain, justified).
- Bundle sizes: 1.9kb / 2.0kb / 2.4kb — consistent with sibling lib/* utilities.
