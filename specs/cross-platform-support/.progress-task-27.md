# Task 1.28 Progress

## Completed Tasks
- [x] 1.28 Create lib/count-mocks.ts - <pending commit>

## Current Task
Awaiting next task

## Learnings
- count-mocks bundle (2.2kb) builds via existing scripts/build-hooks.mjs auto-collection — no entrypoint registration needed.
- Verified mock-pattern regex against synthetic fixture: `{"tests":2,"mockUsages":6,"ratio":3}` matched expected counts (vi.mock, vi.fn, jest.mock, jest.fn, mock.fn all hit).
- Empty / no-test-file dirs correctly emit `{"tests":0,"mockUsages":0,"ratio":0}` without divide-by-zero.
- Sibling parallel tasks 1.26 (ensure-gitignore) and 1.27 (search-files) bundled concurrently in same build run — confirms [P] independence.
