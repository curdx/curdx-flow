# Progress — Task 1.26

## Completed Tasks
- [x] 1.26 Create lib/ensure-gitignore.ts — pending commit

## Current Task
Awaiting next task

## Learnings
- Idempotent append pattern: split on `/\r?\n/`, trim each line, exact-match against entry. No regex over the file body — robust against partial-name collisions (e.g. entry `node_modules` should not collide with `node_modules.bak`).
- Trailing-newline insertion: when raw is non-empty and lacks trailing `\n`, prepend `\n` before appending the new entry+`\n`. Empty existing file → just write `entry+\n`.
- Used `writeFileAtomic` from `_shared/atomic-write.js` (note `.js` for ESM resolution) — same volume, no cross-volume rename risk.
- esbuild auto-collected the new lib file via `build-hooks.mjs` → no manifest edit; bundled to 1.6kb mjs.
- noUncheckedIndexedAccess satisfied by guarding `args[0]` with `=== undefined` plus `length === 0` for empty-string rejection.
