## Task 3.4 — lib unit tests
**Commit**: 26d52c8
**Verify**: `npm run test:hooks` → 14 files / 39 tests passed; `ls tests/hooks/lib/*.test.ts | wc -l | awk '$1>=9'` → `10`, GATE_OK
**Lib-only run**: `npx vitest run tests/hooks/lib` → 10 files / 27 tests, all green (613ms)
**Full suite (with 3.3 already merged)**: 14 files / 39 tests, all green (711ms) — 3.3's 4 hook smoke tests + my 10 lib unit tests = 14 total
**Test layout**:
- `tests/hooks/lib/_lib-helpers.ts` (~85 LOC) — `runLib(name, args, opts)` spawns the bundled `.mjs` lib via `spawnSync('node', [bundle, ...args])`; `makeTmpDir(label)` creates per-test isolated workspaces. Uses ESM `import.meta.url` to derive `REPO_ROOT` (not `__dirname` constant). Independent from 3.3's `_helpers.ts` to avoid race conditions.
- 10 test files (one per lib): cleanup-files (2), count-mocks (2), count-tasks (2), ensure-gitignore (3), get-default-branch (3), init-execution-state (3), kill-port (3), merge-state (3), search-files (3), update-modification-map (3) — total **27 tests**.
**Notes on tricky tests**:
- **kill-port** — happy path = port 65535 (unused) → exit 0 with "no listeners on port 65535". Skipped any actual port-killing test (would need a sacrificial child process listener, brittle in CI). Also covered: invalid-port rejection (0/65536/abc/-1) + missing-arg usage error.
- **get-default-branch** — happy path runs against the curdx-flow repo (has proper `origin/HEAD` → `main`). Negative path runs in a fresh tmp dir (not a git repo) → exit 1. Plus a sanity test that `git --version` works in the test env.
- **count-mocks** — used `tests/` subdir convention to ensure SKIP_DIRS doesn't filter our fixtures. Verified non-test-named files (`not-a-test.ts`) are NOT counted, and that 0-test-files yields ratio=0 (no divide-by-zero).
- **init-execution-state** — happy path tested via fresh tmp dir (no `--force` needed). Also covered `--force` overwrite + the "refuses to overwrite without --force" path. The `loadTemplate()` fallback chain (project-relative → adjacent → embedded) is exercised by the embedded-fallback path since the test runs in tmp dir.
- **cleanup-files** — used cwd override to a tmp dir so the glob expands relative to the test workspace, not the repo root. Verified that `keep.txt` survives while `tmp-*.json` matches are removed. Negative test: absolute path `/etc/passwd` triggers safety guard (warns + skips, exit 0 — best-effort, never aborts).
- **ensure-gitignore** — three cases: create-from-scratch, idempotent-no-op (existing entry), append-with-leading-newline (file lacks trailing `\n`).
- **merge-state** — happy deep-merge + missing-state-file-as-empty + arrays-replace-not-element-merge (catches the `isPlainObject` array-rejection branch).
- **search-files** — three modes: default (`<path>:<line>:<content>`), `--name-only` (one path per matching file), invalid regex (exit 1). Used unique strings per file to verify line-number accuracy.
- **update-modification-map** — covered both arg shapes (spec-dir vs explicit `.json` path) + dedup behavior across multiple invocations on the same task.
**Cross-batch coordination**: 3.3 had already landed by the time I ran tests (4 hook test files + `_helpers.ts` present). My scope (`tests/hooks/lib/`) was strictly disjoint from theirs (`tests/hooks/*.test.ts` + `tests/hooks/_helpers.ts`); no edits needed to their files. Both batches' tests coexist cleanly in a single `npm run test:hooks` invocation.
**No edits to** `.progress.md`, `tasks.md`, `_helpers.ts` (per parallel-mode coordinator policy).
