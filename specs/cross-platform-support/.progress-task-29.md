## Task 1.30 — get-default-branch.ts
**Commit**: <pending>
**Verify**: BRANCH_OK observed (resolved to `main` in this repo)
**Notes**: Used `execFileSync` (not `execSync`) to avoid shell quoting issues on Windows. `git symbolic-ref --short refs/remotes/origin/HEAD` is reliable when the remote HEAD pointer exists; falls back through main → master → first remote branch (excluding HEAD) when not. On detached HEAD locally the symbolic-ref still resolves because we query the remote HEAD ref, not local HEAD.
