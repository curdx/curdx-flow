# Task 30 (3.2 [P]) — update-spec-index test case (NFR-5 c)

## Completed Tasks
- [x] 3.2 [P] Add update-spec-index test case (NFR-5 c)

## Current Task
Done — awaiting batch merge

## Learnings
- The v7.1.0 short-circuit in `update-spec-index.ts` (lines 272-289) is already
  in place from earlier batch work; this RED-style test now passes GREEN.
- Strategy: delete `tasks.md` AND `.progress.md` before invoking the hook.
  Under v7.0.x semantics that wipes every signal `inferPhaseFromFiles` could
  use to produce "completed", so any phase resolution to "completed" must
  come from `state.completed === true`.
- Did NOT use `vi.spyOn` (would require a different module-loading topology
  since the hook runs in a spawned child process via `runHook`); the
  delete-tasks.md approach from the design's NFR-5 case (c) note works
  correctly for spawn-based test harnesses.
