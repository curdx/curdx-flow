# Progress — task 1.4 (merge-state $unset unit tests)

## Completed Tasks
- [x] 1.4 Add 6 $unset unit tests (U-1..U-6) to tests/hooks/lib/merge-state.test.ts

## Current Task
Done.

## Learnings
- Existing harness in `tests/hooks/lib/_lib-helpers.ts` (`runLib`, `makeTmpDir`) was reused — spawns the bundled `.mjs` directly, so this test file *will* fail until sibling task 1.2 ships the `$unset` impl AND 1.5 [VERIFY] re-bundles. Per task instructions, did NOT run `npm run test:hooks`.
- All 6 cases follow the existing `tmpdir + try/finally rmSync` pattern; assertions use both `toEqual` and `"key" in after` to verify true key deletion (vs `undefined` value).
- U-4 also asserts the base state file is untouched on validation failure — guards against accidental partial writes.
- `npm run typecheck` passed (compile-only sanity); functional verification deferred to task 1.5.
